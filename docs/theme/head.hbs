{{!-- Glide theme custom head.
     To show a version pill next to the book title in the header, add:
       <meta name="glide-version" content="v1.0">
     The theme's glide.js reads it; omit it to hide the pill. --}}
