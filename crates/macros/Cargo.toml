[package]
name = "umari-macros"
version = "0.2.0"
edition = "2024"
description = "Derive macros for umari (CommandInput, Event, EventSet)"
license.workspace = true

[lib]
proc-macro = true

[dependencies]
heck.workspace = true
proc-macro2.workspace = true
quote.workspace = true
syn = { workspace = true, features = ["derive", "full"] }
